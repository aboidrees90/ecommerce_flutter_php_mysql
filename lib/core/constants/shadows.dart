import 'package:flutter/material.dart';

const glowingShadow = BoxShadow(offset: Offset(1, 1), spreadRadius: -1, blurRadius: 0.5, color: Colors.black);
const shadowDown = BoxShadow(offset: Offset(1, 1), spreadRadius: -1, blurRadius: 0.5, color: Colors.black);
const shadowUp = BoxShadow(offset: Offset(1, -1), spreadRadius: -2, blurRadius: 3, color: Colors.black);
